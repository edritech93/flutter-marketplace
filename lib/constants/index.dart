const String BASE_URL = 'http://jsonplaceholder.typicode.com/';

const String APP_NAME = 'Marketplace';

const String STORAGE_TOKEN = 'STORAGE_TOKEN';

const String STORAGE_REFRESH_TOKEN = 'STORAGE_REFRESH_TOKEN';

const String STORAGE_THEME = 'STORAGE_THEME';

const String TABLE_FETCH = 'TABLE_FETCH';

enum MethodApi { get, post, put, delete }
